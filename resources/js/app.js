// Import file bootstrap.js bawaan Laravel yang biasanya hanya menginisialisasi axios dan beberapa hal lainnya
import './bootstrap';

// Impor seluruh Bootstrap JavaScript. Ini penting untuk fitur interaktif seperti navbar toggle, carousel, modal, dll.
import 'bootstrap/dist/js/bootstrap.bundle.min';

// Kode JavaScript kustom Anda bisa ditempatkan di sini
// Contoh sederhana:
document.addEventListener('DOMContentLoaded', function() {
    console.log('App.js loaded and DOM is ready!');
    // Tambahkan kode JS kustom Anda di sini jika ada
});